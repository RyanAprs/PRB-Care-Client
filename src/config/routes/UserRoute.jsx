import { BrowserRouter as Router, Routes, Route } from "react-router-dom";

const UserRoute = () => {
  return (
    <Router>
      <Routes>
        <Route />
      </Routes>
    </Router>
  );
};

export default UserRoute;
